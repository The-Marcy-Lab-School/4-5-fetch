/* 
Fetch data from this API endpoint and render it to the DOM!
*/

const API_ENDPOINT = "https://v2.jokeapi.dev/joke/Programming?blacklistFlags=nsfw,religious,political,racist,sexist,explicit&type=twopart";

const fetchButton = document.querySelector('#fetch-button');
fetchButton.addEventListener('click', (event) => {
  /* 
  TODO:
  1. invoke fetch
  2. define .then and .catch handlers
  3. throw an error if the response is not ok
  4. read the response body
  5. use the response body data and render it
  6. handle errors
  */
})